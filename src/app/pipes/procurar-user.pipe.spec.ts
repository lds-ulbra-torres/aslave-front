import { ProcurarUserPipe } from './procurar-user.pipe';

describe('ProcurarUserPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcurarUserPipe();
    expect(pipe).toBeTruthy();
  });
});
